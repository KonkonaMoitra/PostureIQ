# PostureIQ
AI Interview Posture Analyzer is a Flask-based web application that uses MediaPipe pose detection to analyze a user’s posture during interview practice. It evaluates body alignment in real time via webcam &amp; provides feedback on slouching or imbalance. It system includes secure authentication &amp; SQLite storage to track posture improvement over time.
